1. copy demo directory (it is in example.uz/php directory) into html directory

2. run following command (in docker directory) to build docker image

docker build -t e-imzo-demo:latest .

3. configure your nginx respectively